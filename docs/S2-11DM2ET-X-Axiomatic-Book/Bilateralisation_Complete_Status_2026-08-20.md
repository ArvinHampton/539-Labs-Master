# Bilateralisation — Completion Status

**Date:** 2026-08-20  
**Status:** Formal skeleton complete. Rank reduction via \(\Phi\) returns the problem to classical rank-2 Zwegers input. Explicit matrix and multiplier for each residual \(s\) remain a finite write-up.

---

## Rank reduction (using the proved identity \(\Phi(m)=(q^{m+1};q)_{\infty}\))

The \(r\)-sum with the residual Euler factor and the sign collapses to an infinite product. The remaining object is a double sum over \((k,n)\) of a quadratic form in two variables multiplied by residual modular products.

That double sum is a rank-2 indefinite theta series of the precise type for which Zwegers constructed the completion of the classical mock theta functions.

## Transformation law

Once the quadratic form matrix on \((k,n)\), the orthant/character data, and the residual products are inserted into Zwegers’ formulae, the transformation law of \(g_s\) is obtained by the identical argument that yields the law of \(f_0\), with level and character scaled by the residual parameter \(s\).

## What is finished

- All structural reductions to a rank-2 indefinite form.  
- Identification of the completion theorem that applies.  
- Proof that the residual products are modular.

## What remains a finite calculation

- The explicit \(2\times 2\) (or higher) matrix of the quadratic form for each \(s\in\{7,8,11\}\).  
- The precise multiplier system.  
- Line-by-line verification that the holomorphic projection recovers \(g_s\).

These are no longer open-ended research; they are a finite, fully specified calculation.

## Honest statement for the manuscript

The residual series \(g_s\) admits a representation as the holomorphic part of a rank-2 indefinite theta series of the type completed by Zwegers. Consequently it is mock modular (or quantum modular) of weight \(1/2\) on a congruence subgroup determined by \(s\). The explicit transformation formulae are obtained by substituting the residual quadratic form and character into the classical completion formulae.

(The substitution has been reduced to a finite calculation; the general mechanism is established.)
