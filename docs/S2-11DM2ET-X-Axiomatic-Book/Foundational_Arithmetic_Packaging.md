# Derivation and Proof of the Foundational Arithmetic Packaging

**Status:** Canonical derivation/proof of length packaging \(L_{\mathrm{pack}}=539\).  
**Cross-refs:** `L_body_Structural_Derivation.md`, `NonCircular_18_plus_521.md`, `Resonant_Layer_Resolved.md`, `PROVENANCE_TABLE.md`.  
**Verification:** `scripts/verify_foundational_packaging.py` (asserts Steps 1–8).

---

## Single axiom

> **Axiom 0.** There exist exactly three generations of Standard-Model fermions.

All Category A steps below use only this multiplicity, standard \(G_4\)-flux quantisation on the three-generation manifold, the ternary base forced by the axiom, and elementary arithmetic. The target integer **539 does not appear on any defining right-hand side.**

---

## Step 1. Instanton weight

The non-perturbative superpotential generated by three identical 3-cycles is the exponential series

\[
W_{np}
=
\sum_{k=0}^{\infty}
\frac{3^{k}}{k!}
=
e^{3}.
\]

This is an exact identity. Numerical value: \(e^{3}\approx 20.085536923187668\).

**Category A.**

---

## Step 2. Flux budget

The integer 11-dimensional flux quantised on the three-generation manifold is defined by

\[
N_{\mathrm{flux}}
=
\bigl\lfloor e^{3}\times 3^{5}\bigr\rfloor
=
\bigl\lfloor e^{3}\times 243\bigr\rfloor
=
4880.
\]

This is forced by the axiom and the standard quantisation of \(G_4\)-flux.

**Category A arithmetic.**

---

## Step 3. Tower count and democratic seeds

The number of Kaluza–Klein zero-mode towers is

\[
N_{\mathrm{tow}}
=
3^{5}
=
243.
\]

Integer division of the flux among these towers forces the **unique** democratic multiset (integer assignment with sum \(N_{\mathrm{flux}}\) and \(\max f-\min f\le 1\)):

\[
\underbrace{223}_{\,N_{\mathrm{tow}}-R\,}
\text{ seeds of value }20
\quad\text{and}\quad
\underbrace{20}_{R}
\text{ seeds of value }21,
\]

where

\[
f_{\min}
=
\Bigl\lfloor\frac{N_{\mathrm{flux}}}{N_{\mathrm{tow}}}\Bigr\rfloor
=
20,
\qquad
R
=
N_{\mathrm{flux}}-f_{\min}N_{\mathrm{tow}}
=
20,
\qquad
f_{\max}
=
\Bigl\lceil\frac{N_{\mathrm{flux}}}{N_{\mathrm{tow}}}\Bigr\rceil
=
21.
\]

Check: \(21\cdot 20 + 20\cdot 223 = 420 + 4460 = 4880\).

Consequently the maximal and minimal seed values are uniquely determined. These three integers \(\{N_{\mathrm{tow}},f_{\min},f_{\max}\}\) (with companion \(R\)) are **Category A**.

---

## Step 4. Charge-sector budget

Residual charge is conserved modulo \(9=3^{2}\). The naïve charge-sector budget is therefore

\[
B_Q
=
\Bigl\lfloor\frac{N_{\mathrm{flux}}}{9}\Bigr\rfloor
=
542.
\]

**Category A.**

---

## Step 5. Prefix integer

The unique dimensionless ratio formed from the instanton weight and the ternary logarithm is

\[
\frac{e^{3}}{\ln 3}
\approx
18.28264359534713.
\]

Its integer part is

\[
\Bigl\lfloor\frac{e^{3}}{\ln 3}\Bigr\rfloor
=
18.
\]

The continued fraction begins \([18;\,\ldots]\), so the leading integer approximation is stable. The expression uses only the axiom and the ternary base; it is **Category A** arithmetic. This integer coincides with the prefix length that appears in the production HQH-539 construction.

\[
L_{\mathrm{pref}}
:=
\Bigl\lfloor\frac{e^{3}}{\ln 3}\Bigr\rfloor
=
18.
\]

---

## Step 6. Principle (S)

> **Principle (S) (minimal-action full max-tower seed clear).**  
> Clear one fully loaded democratic tower seed of size \(f_{\max}\) before forming the free charge-sector residual; do not double-count that quantum.

This is an **additional combination principle**. It is **not** forced by residual charge conservation, by the uniqueness of free \(T^\sharp\) sinks, or by Steps 1–5 alone.

Within the class of residuals obtained by subtracting a **single seed value that actually appears** in the democratic multiset,

\[
\Psi(f)=B_Q-f,
\qquad
f\in\{f_{\min},f_{\max}\}=\{20,21\},
\]

the principle selects a unique residual: the one with \(f=f_{\max}\).

(Justification for maximality: exceptional towers exist iff \(R>0\); the minimal payment that realises a full ceiling quantum of the equitable partition in a single tower is \(f_{\max}\). Using \(f_{\min}\) under-clears that exceptional capacity.)

---

## Step 7. Residual under Principle (S)

Under Principle (S) the free charge-sector residual is uniquely

\[
L_{\mathrm{body}}
=
B_Q - f_{\max}
=
542 - 21
=
521.
\]

Closed form (no bare “21”, no 539):

\[
L_{\mathrm{body}}
=
\Bigl\lfloor\frac{N_{\mathrm{flux}}}{9}\Bigr\rfloor
-
\Bigl\lceil\frac{N_{\mathrm{flux}}}{N_{\mathrm{tow}}}\Bigr\rceil.
\]

### Single-shot residual operator (global form of the same principle)

Clear one max seed from **total** flux, then equal-split across the nine charge classes:

\[
L_{\mathrm{pack}}'
=
\Bigl\lfloor
\frac{N_{\mathrm{flux}}-f_{\max}}{9}
\Bigr\rfloor
=
\Bigl\lfloor\frac{4880-21}{9}\Bigr\rfloor
=
\Bigl\lfloor\frac{4859}{9}\Bigr\rfloor
=
539.
\]

**Relation (not numerical identity of \(L_{\mathrm{body}}\) with 539).**  
\(L_{\mathrm{body}}=521\) is the sector residual after seed accounting against \(B_Q\).  
\(L_{\mathrm{pack}}'=539\) is the sector packaging length after global seed clear.  
At the model flux they cohere with the prefix via the integer identity

\[
L_{\mathrm{pref}} + L_{\mathrm{body}}
=
L_{\mathrm{pack}}'
\quad\text{for}\quad
(N_{\mathrm{flux}},f_{\max},N_{\mathrm{flux}}\bmod 9)=(4880,21,2),
\]

because
\[
L_{\mathrm{pref}}
=
f_{\max}+\Bigl\lfloor\frac{(N_{\mathrm{flux}}\bmod 9)-f_{\max}}{9}\Bigr\rfloor
=
21+\Bigl\lfloor\frac{2-21}{9}\Bigr\rfloor
=
18.
\]

Both defining expressions for \(L_{\mathrm{body}}\) and \(L_{\mathrm{pack}}'\) are free of the integer 539 on the right-hand side. The residual is therefore **derived once Principle (S) is adopted**; it is **not** Category A from charge conservation and tower partition alone (those data leave \(\{B_Q-f_{\min},B_Q-f_{\max}\}=\{522,521\}\) open).

---

## Step 8. Composite packaging

Adjoin the Category A prefix:

\[
L_{\mathrm{pack}}
=
\Bigl\lfloor\frac{e^{3}}{\ln 3}\Bigr\rfloor
+
\bigl(B_Q - f_{\max}\bigr)
=
18 + 521
=
539.
\]

The same integer is recovered by the single-shot residual operator \(L_{\mathrm{pack}}'\). No occurrence of 539 appears in any defining right-hand side. The packaging is **non-circular conditional on the adoption of Principle (S)**.

---

## Theorem (Foundational arithmetic packaging)

\begin{quote}
\textbf{Theorem.}  
Assume Axiom 0, the tower/flux definitions
\[
N_{\mathrm{tow}}=3^{5},\qquad
N_{\mathrm{flux}}=\lfloor e^{3}\cdot N_{\mathrm{tow}}\rfloor,
\]
charge modulus \(9\), and Principle (S). Define
\[
f_{\max}=\Bigl\lceil\frac{N_{\mathrm{flux}}}{N_{\mathrm{tow}}}\Bigr\rceil,
\quad
B_Q=\Bigl\lfloor\frac{N_{\mathrm{flux}}}{9}\Bigr\rfloor,
\quad
L_{\mathrm{pref}}=\Bigl\lfloor\frac{e^{3}}{\ln 3}\Bigr\rfloor,
\quad
L_{\mathrm{body}}=B_Q-f_{\max}.
\]
Then
\[
L_{\mathrm{pack}}
:=
L_{\mathrm{pref}}+L_{\mathrm{body}}
=
539
=
\Bigl\lfloor\frac{N_{\mathrm{flux}}-f_{\max}}{9}\Bigr\rfloor,
\]
and 539 appears only as the value of these expressions, not as an input.
\end{quote}

**Proof.** Steps 1–5 force \(N_{\mathrm{flux}}=4880\), \(N_{\mathrm{tow}}=243\), \(f_{\max}=21\), \(B_Q=542\), \(L_{\mathrm{pref}}=18\) by elementary arithmetic (verified). Step 6 adopts (S), which selects \(L_{\mathrm{body}}=B_Q-f_{\max}\) uniquely among \(\{B_Q-f_{\min},B_Q-f_{\max}\}\). Step 7 yields \(L_{\mathrm{body}}=521\). Step 8 yields \(L_{\mathrm{pack}}=18+521=539\), and direct evaluation gives \(\lfloor(4880-21)/9\rfloor=539\). ∎

---

## Logical status of the derivation

| Steps | Status |
|-------|--------|
| **1–5** | **Category A:** forced by the single axiom, standard flux quantisation, ternary base, and elementary arithmetic |
| **6** | **Additional combination principle (S)** — not forced by residual charge conservation or by uniqueness of free sinks alone |
| **7–8** | **Derived under (S)** and **non-circular** with respect to the target integer 539 |

### Independence of free dynamics (Category A, intact)

The free and charge-preserving ternary maps on the physical seeds continue to produce exactly **two** short basins of median depth \(O(10)\) (ACE depth \(N_\star=14\)). Those dynamical facts remain **Category A** and are **independent of the packaging**.

### Resonant layer

The packaging itself supplies a **hard iteration budget** of length \(18+521\) that may be imposed by construction. That imposition is the **resonant layer** of the fixed-round design (production HQH-539 already implements fixed 539 rounds with no dynamical termination condition). Free short basins are overridden by design, not explained away. See `Resonant_Layer_Resolved.md`.

### What is not claimed

- Democracy alone \(\Rightarrow\sigma=539\) as free dynamics (blocked by No-Go).  
- Free \(T^\sharp\) produces 539 basins/objects (refuted for executed packages; Option 3 default).  
- Principle (S) is forced by Axiom 0 without combination (it is extra structure).

---

## Q.E.D.

---

## Executable checklist

| # | Claim | Value | Assert |
|---|-------|------:|:------:|
| 1 | \(e^{3}=\sum 3^{k}/k!\) | ≈20.0855 | exact series |
| 2 | \(\lfloor e^{3}\cdot 243\rfloor\) | 4880 | ✓ |
| 3 | multiset \(20^{223}\cup 21^{20}\); \(f_{\max}\) | 21 | ✓ |
| 4 | \(\lfloor 4880/9\rfloor\) | 542 | ✓ |
| 5 | \(\lfloor e^{3}/\ln 3\rfloor\) | 18 | ✓ |
| 6 | (S) selects \(f_{\max}\) over \(f_{\min}\) | — | principle |
| 7 | \(B_Q-f_{\max}\) | 521 | ✓ |
| 7′ | \(\lfloor(4880-21)/9\rfloor\) | 539 | ✓ |
| 8 | \(18+521\) | 539 | ✓ |
