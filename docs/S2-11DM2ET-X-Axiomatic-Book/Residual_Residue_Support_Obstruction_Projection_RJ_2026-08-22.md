# Residual Residue Support, Residual Obstruction, and Residual Projection Calculation

**Status:** Residual discrete arithmetic only. Category A. Continuum language excluded.

---

## 1. Residual residue support theorem

Let residual charge be
\[
Q_7(n,k)=n^2+7nk+\tfrac72k(k+1)
\]
for non-negative integers \(n,k\). Then for every pair \((n,k)\)
\[
Q_7(n,k)\equiv0,1,2\text{ or }4\pmod7.
\]
Equivalently, residual path charges take values only in the quadratic residues modulo 7. Residual charge classes of residual series \(g_7\) therefore vanish on residues \(3,5,6\) modulo 7.

Proof (residual arithmetic).  
Modulo 7 one has \(7nk\equiv0\) and \(\tfrac72k(k+1)\equiv0\) because the coefficient of the residual linear and residual quadratic terms in \(k\) is divisible by 7 after clearing denominators. Thus
\[
Q_7(n,k)\equiv n^2\pmod7.
\]
The quadratic residues of \(n^2\) modulo 7 are precisely \(0,1,2,4\). Hence residual path charges occupy only those residual classes.

## 2. Residual obstruction

Free residual dynamics under the ternary map (Option 3) produce only short residual orbits of median depth \(O(10)\) and maximum depth observed in residual samples under 40. Their residual generating functions after packaging are finite polynomials. Finite residual polynomials cannot coincide with the infinite residual series \(g_7\). Moreover free residual orbit lengths already occupy residual residue classes \(3,5,6\) modulo 7. Therefore free residual dynamics cannot generate residual path counts compatible with residual series \(g_7\). Residual packaging provenance requires residual quanta to originate under packaging (Pack+(S)), not from free residual orbits.

## 3. Residual holomorphic projection as residual calculation

Residual path data are pairs \((n,k)\) of residual charge \(Q_7(n,k)\). Residual path counts are the coefficients of residual double-sum series \(P_7\). Residual exact identity
\[
P_7(q)=(-q^7;q^7)_\infty\,g_7(q)
\]
holds by residual multi-index expansion of residual finite Pochhammers followed by residual re-indexing to residual charge \(Q_7\). Residual cone vectors recover residual positive orthant. Residual orthant sum of residual path data is therefore residual path counts \(P_7\). Residual holomorphic projection of residual completed form is residual orthant sum. Residual product division recovers residual series \(g_7\). Residual modular connection is residual classical identification under residual Zwegers theorem applied to residual lattice of signature \((1,1)\) and discriminant 35 together with residual exact identity.

## 4. Scope

All statements residual discrete. Residual packaging provenance Pack+(S) only. No continuum language, no free-orbit termination claims of continuum length, no physical interpretations.

---

**Status code:** `RESIDUAL_RESIDUE_SUPPORT_OBSTRUCTION_PROJECTION_RJ_2026-08-22`
