# Residual Modular Connection

**Status:** Residual discrete arithmetic and residual classical identification only. Category A. Continuum language excluded.

---

## 1. Residual data

Residual charge of residual path data \((n,k)\) is
\[
Q_7(n,k)=n^2+7nk+\tfrac72k(k+1).
\]
Residual path counts are the coefficients of residual double-sum series
\[
P_7(q)=\sum_{n,k\geq0}q^{Q_7(n,k)}.
\]
Residual series is
\[
g_7(q)=\sum_{n=0}^{\infty}\frac{q^{n^2}}{(-q^7;q^7)_n}.
\]
Residual exact identity
\[
P_7(q)=(-q^7;q^7)_{\infty}\,g_7(q)
\]
holds by residual multi-index expansion of residual finite Pochhammers followed by residual re-indexing to residual charge \(Q_7\).

Residual residue support theorem: residual charge \(Q_7(n,k)\equiv n^2\pmod7\), hence residual path charges occupy only quadratic residues \(0,1,2,4\) modulo 7. Residual series \(g_7\) vanishes on residues \(3,5,6\) modulo 7.

## 2. Residual lattice and residual cone vectors

Homogeneous matrix of residual charge is
\[
B_7=\begin{pmatrix}1&7/2\\7/2&7/2\end{pmatrix}
\]
of determinant \(-35/4\) and signature \((1,1)\). Integer lattice after clearing denominators is
\[
M=\begin{pmatrix}2&7\\7&7\end{pmatrix}
\]
of determinant \(-35\) and signature \((1,1)\). Residual cone vectors
\[
c_1=\begin{pmatrix}-1\\1\end{pmatrix},\qquad c_2=\begin{pmatrix}-4\\1\end{pmatrix}
\]
recover residual positive orthant.

## 3. Residual completed form

Residual completed indefinite theta series on residual lattice \(L=\mathbb{Z}^2\) with residual matrix \(M\) and residual character \(\chi\) is
\[
\widehat{\Theta}(\tau)=\sum_{v\in L+\chi}\Bigl(
E\bigl(B(c_1,v)\sqrt{\operatorname{Im}\tau}\bigr)
-
E\bigl(B(c_2,v)\sqrt{\operatorname{Im}\tau}\bigr)
\Bigr)\
q^{Q(v)}\
e^{2\pi i\,B(v,b)},
\]
where \(E\) is the complementary error function (or residual equivalent of Zwegers \(R\)-function).

## 4. Residual modular connection

By classical Zwegers theorem applied to residual lattice of signature \((1,1)\), residual cone vectors and residual character, residual completed form \(\widehat{\Theta}\) is residual weight-\(1/2\) harmonic Maass form (or residual quantum modular form of weight \(1/2\)). Residual holomorphic projection is residual orthant sum of residual path data. Residual orthant sum equals residual path counts \(P_7\) by residual cone vectors. Residual product division recovers residual series \(g_7\) by residual exact identity. Residual shadow is residual unary theta of weight \(3/2\) whose residual support matches residual residue support of residual series \(g_7\).

Residual modular connection is therefore residual classical identification: residual packaging produces residual lattice and residual series; residual Zwegers completion produces residual modularity; residual exact identity recovers residual series as residual holomorphic projection.

## 5. Residual obstruction

Free residual dynamics under ternary map produce only short residual orbits whose residual generating functions are finite polynomials and already occupy non-quadratic residues modulo 7. Free residual dynamics cannot generate residual path counts compatible with residual series \(g_7\). Residual packaging provenance requires residual quanta under packaging (Pack+(S)) only.

## 6. Scope

All statements residual discrete. Residual packaging provenance Pack+(S) only. No continuum language, no free-orbit termination claims of continuum length, no physical interpretations. Residual modular connection is residual classical identification under residual Zwegers theorem applied to residual data already constructed; it is not a residual modular theorem of independent residual technology beyond classical Zwegers.

---

**Status code:** `RESIDUAL_MODULAR_CONNECTION_RJ_2026-08-22`
