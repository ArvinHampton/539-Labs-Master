Per_off(Xi) is not Dual-split Abel
Identification closed negative
2026-09-02

Pack+(S). Residual-flux provenance mandatory. RESIDUAL_CORE_FREEZE holds.
Category A residual discrete only. Continuum Category B.
Twin Prime and Riemann Hypothesis unclaimed.
A count is not a clock. Residual is not Resonant.
This note does not populate Per_off(Xi).
This note does not close Remark 8.5.


0. What was asked

Is the Dual-split Abel table of 29 August already Per_off(Xi)?
If yes, the header fills by identification.
If no, write why and leave the header.


1. The two definitions

Per_off(Xi), written 2 September.

Independent residual discrete table of h_residual^high on a residual-rational grid disjoint from {1/7, 3/7, 5/7, 1} union W_7 of that set, generated only from locked coefficients of Xi and from the written incomplete-gamma kernel, without using residual_slash to define the values.

Header only. Table empty.

Dual-split Abel table, written 29 August.

Signed Dual channels of Xi, of C_axis, and of Theta_third, Abel-regularised kernel N^{-1/2} exp(-2 pi N epsilon) with epsilon = 10^{-4}, evaluated at the four samples, their Fricke images, and the positive 1/14 grid.

Table populated. File Independent_H_Table_Dual_Split_Pslash_2026-08-29.md.


2. Why they are not the same object

Kernel. Per_off asks for incomplete-gamma / error-function. Dual-split Abel uses Abel N^{-1/2} with a numerical regulator.

Series set. Per_off asks for Xi only. Dual-split Abel uses Xi plus C_axis plus Theta_third.

Purpose. Per_off is an off-sample writing of generator G3 alone. Dual-split Abel was built to test whether a span of periods realises Object A and residual_slash.

P-Xi already recorded that Dual-split Abel and incomplete-gamma are two writings of the same leading kernel on the same Xi modes. Two writings are not one table. Matching them off-sample is OP-Xi-grid, still open. That match would identify G3 with itself. It would not put the half-integer Xi_odd ridge into Period(C_axis) or Period(Theta_third holomorphic). It would not make residual_slash two-sided.


3. What this closes

Identification Dual-split Abel = Per_off(Xi) is closed negative.

The populated sibling may be named Dual_Abel_off.
It is the Xi channels of the 29 August table, already on the 1/14 grid.
Naming Dual_Abel_off does not populate Per_off.
Naming Dual_Abel_off does not close Remark 8.5.

Per_off stays a header.
OP-Xi-grid stays open as a comparison of two writings of G3.
P-Xi term-by-term identification of Eichler(Xi) with Period(C_axis) plus Period(Theta_third holomorphic) stays closed negative from this morning.


4. What this note does not do

It does not evaluate a new incomplete-gamma grid.
It does not fit a fifth slash coefficient.
It does not identify residual_slash with Auto.
It does not treat G3 as G4.


Status codes.

PER_OFF_NOT_DUAL_ABEL_2026-09-02
DUAL_ABEL_OFF_ALREADY_POPULATED_29_AUG
PER_OFF_HEADER_ONLY
OP_XI_GRID_STILL_OPEN
REMARK_85_NOT_CLOSED_BY_IDENTIFICATION

CORE_FREEZE unchanged.
Residual-flux provenance mandatory.
