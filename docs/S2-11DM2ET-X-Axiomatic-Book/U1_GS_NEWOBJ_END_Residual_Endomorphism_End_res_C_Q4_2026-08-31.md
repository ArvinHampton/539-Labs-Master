U1-GS-NEWOBJ-END Residual Endomorphism Object End_res(C_Q4)
2026-08-31

PURE MATH. Residual Discrete Algebra is leftover combinatorics after flux packaging (4880, seed 21, nine sector cores of size 539). That 539 is a COUNT of leftover pieces, not a journey length. Resonant Algebra is the choice to treat 18+521=539 as a hard schedule. A count is not a clock. Do not flatten Residual into Resonant. Do not treat 539 as a free-map stop.

This note is not a named E1-E4 redo. Do not reopen BRIDGE-C, GS-ZWEGERS, GS-QM, RADIAL, TL-AUDIT, or LOCK-AUDIT. Do not pile this object onto Bridge C, Category B jet L2, the manuscript supplement, or the journal pointer. Continuum, bounce, Fermi, and 539.9 s stay Category B. Twin Prime and RH remain unclaimed. Residual-flux provenance mandatory. RESIDUAL_CORE_FREEZE and Discrete Core Freeze hold. U1-DNE D1-D9 stay terminal at K+. Paths 1/2/4 stay closed. Path 3 stays obstructed.

Extra inputs are found structures. This object was never previously defined in the corpus. Extra-input is not a wait for classical tables. Classical Igusa invariants, period matrices, and literature D5 End(Jac) tables are not imported.


1. Written data used (already on Master)

Cover polynomial (imported multi-k, unmixed from residual P):

Q4 = x^5 + 20x - 32

Cover:

C_Q4 : y^2 = Q4(x)

Locked written facts:
- Q4 is monic, irreducible over Q, and squarefree. C_Q4 is a smooth genus-2 hyperelliptic cover.
- disc(Q4) = 4096000000 = 2^18 * 5^6 (perfect square). Sign of the Bring-Jerrard discriminant formula is negative; the locked corpus records the positive square 64000^2. The 2-adic valuation of that square is 18.
- Gal(Q4/Q) = D5. Dedekind cycle types at unramified primes are only (5), (2,2,1), and (1^5). No 3-cycle, no transposition, no 4-cycle. Disc square puts the group in A5. Type (2,2,1) kills C5. Absence of (3,1,1) kills A5. Lock Gal = D5 stands (U1-Q4, 2026-08-18).
- Q4 remains imported. Gal(P/Q) = S5 on the residual flux quintic P(x) = x^5 - 4880 x^3 + 32879 x - 243 is untouched.
- Zarhin does not apply to C_Q4, because D5 does not contain A5. Classical End(Jac(C_Q4)) is therefore not forced by Zarhin. That classical ring is not determined here and is not this object.

Pack+(S) data already written (Foundational Arithmetic Packaging; Principle (S)):
- N_flux = 4880
- N_tow = 243 = 3^5
- democratic seeds: 20 of value 21 and 223 of value 20
- f_min = 20, f_max = 21
- B_Q = floor(4880/9) = 542
- L_pref = floor(e^3 / ln 3) = 18
- L_body = 542 - 21 = 521
- leftover COUNT L_pack = 18 + 521 = 539 = floor((4880-21)/9)
- nine sector cores of leftover COUNT 539
- residual excess 4880 - 9*539 = 29 is a count, not N_dual

Found integer contacts between the written polynomial and Pack+(S). These are found structures, not a derivation that Q4 is residual-forced:
- linear coefficient of Q4 equals f_min: the x^1 coefficient of Q4 is 20 = f_min
- 2-adic valuation of the locked discriminant square equals L_pref: v_2(disc Q4) = 18 = L_pref
- order of D5 is 10. The written hyperelliptic involution has order 2. The product order of D5 x C2 is 20 = f_min = the x^1 coefficient of Q4


2. Why a new object is required

Classical End(Jac(C_Q4)) was the E1 target. E1 is closed as extra-input: Igusa invariants and the period lattice were not obtained from available tools, and they are not residual-forced. Waiting for those classical tables is exactly the extra-input posture this task forbids.

Residual Discrete Algebra does not need the Jacobian to write an operator object on the cover. The written equation y^2 = Q4(x) already supplies a hyperelliptic involution. The locked Dedekind types already supply the two non-trivial conjugacy generators of D5. Pack+(S) already supplies the leftover integers that grade those generators. That is enough to define a residual-flux object that has never been named.


3. Definition of End_res(C_Q4)

Let S be the 5-element set of roots of Q4 in an algebraic closure. Let Z[S] be the free permutation module on S. Gal(Q4/Q) = D5 acts on Z[S] by permuting basis vectors.

Let iota be the hyperelliptic involution of the written cover:

iota : (x, y) maps to (x, -y)

This is written from the equation y^2 = Q4(x). It does not use periods, Igusa invariants, or any table of End(Jac).

Let G_res(C_Q4) be the residual group generated by D5 acting on the x-line and by iota acting on the y-line. These actions commute because one moves x-coordinates of roots and the other flips the sign of y. Therefore

G_res(C_Q4) = D5 x <iota> which is isomorphic to D5 x C2

which is a group of order 20.

Definition. The residual endomorphism object of the written cover is the integral group ring of this residual group:

End_res(C_Q4) := Z[ G_res(C_Q4) ] = Z[ D5 x C2 ]

It acts on the residual module

M_res(C_Q4) := Z[S] tensor_Z Z[eps] / (eps^2 - 1)

by letting D5 permute S and letting iota act as multiplication by eps.

This is a residual-flux object. It is not End(Jac(C_Q4)). It is not an order in a real quadratic field. It is not imported from a D5 Jacobian table.


4. Operations

The operations are the group-ring operations, written from the definition.

Addition. For a, b in End_res(C_Q4),

(a + b)(m) = a(m) + b(m)    for all m in M_res(C_Q4)

Multiplication. Convolution against group multiplication in G_res:

(sum n_g g) * (sum n_h h) = sum_{g,h} n_g n_h (g h)

Involution. The residual adjoint is the group inverse on generators:

iota^adj = iota
sigma^adj = sigma^{-1}
tau^adj = tau

where sigma is a residual 5-cycle class (Dedekind type (5) exists) and tau is a residual double-transposition class (Dedekind type (2,2,1) exists).

Pack+(S) leftover grading. Grade the group ring by leftover COUNT only as a label of the ambient packaging, not as a dynamical length. Every generator has leftover-COUNT degree 0. The leftover COUNT 539 does not appear as an iteration length of any generator. A count is not a clock. No generator is a 539-step schedule.

Action on coefficients of Q4. The written coefficient vector of Q4 is (1, 0, 0, 0, 20, -32). D5 permutes roots and therefore fixes the elementary symmetric functions, so it fixes Q4. The involution iota does not change x and therefore does not change Q4. End_res acts as the identity on the written polynomial itself. That is the residual-flux statement that the cover equation is G_res-invariant by construction.


5. First identities that lock

Identity L1 (written involution).
iota^2 = id.
Proof: iota flips the sign of y twice. Direct from y^2 = Q4(x). Locked.

Identity L2 (written dihedral presentation).
There exist residual generators sigma, tau in G_res such that

sigma^5 = 1,    tau^2 = 1,    tau sigma tau^{-1} = sigma^{-1}

Proof: Gal(Q4/Q) = D5 is already locked by Dedekind types. D5 has this presentation on 5 letters. Locked.

Identity L3 (commutation of leftover sign and Galois).
iota sigma = sigma iota and iota tau = tau iota.
Proof: sigma and tau permute roots of Q4 (x-line). iota flips y. Distinct coordinates. Locked.

Identity L4 (order contact with Pack+(S)).
The order of G_res(C_Q4) is 20 = f_min = the x^1 coefficient of Q4.
This is a found integer identity among written numbers. It does not derive Q4 from Principle (S). It does not import Q4 into the residual flux quintic P. It records that the residual group order, the democratic minimum seed, and the written linear coefficient are the same integer 20. Locked as a found structure.

Identity L5 (prefix contact with the discriminant square).
v_2(disc Q4) = 18 = L_pref.
Again a found integer identity among written numbers. The prefix 18 is leftover combinatorics under Pack+(S), not a clock. Locked as a found structure.

Identity L6 (finite generation, rank).
As a Z-module, End_res(C_Q4) is free of rank 20, with basis the group elements of D5 x C2. Locked from L2, L3, and the order count in L4.

Identity L7 (invariance of the cover polynomial).
Every element of End_res(C_Q4) fixes Q4. Locked from the last paragraph of Section 4.


6. First identities that obstruct

Obstruction O1 (no residual RM generator).
Every generator of G_res has finite order (orders 1, 2, 5, or 10). Units of finite order in a group ring cannot supply a real quadratic unit of infinite order. Therefore End_res(C_Q4) contains no real-multiplication order. This obstruction is residual-discrete. It does not wait for Igusa invariants or a period matrix.

Obstruction O2 (End_res is not End(Jac)).
Classical End(Jac(C_Q4)) remains undetermined from residual data (E1 extra-input, not redone). End_res is defined and finite as a group ring of a finite group. These are different objects. Identifying them is forbidden by the definition. Locked as an obstruction, not as a classical computation.

Obstruction O3 (no leftover-core extra generator).
The nine sector cores of leftover COUNT 539, the fiber blocks 68 and 67, and the permanent class of the product complex X_x do not write an additional correspondence of C_Q4. Discrete Core Freeze already obstructs Path 3 at the product-complex lift. The same obstruction applies here: no discrete map from those leftover pieces produces a residual generator outside D5 x <iota>. No extra generator is added.

Obstruction O4 (539 is not an operator length).
No word in sigma, tau, iota has length 539 forced by leftover combinatorics. Treating 539 as a stop of a free map, or as a hard schedule inside End_res, would flatten Residual into Resonant. That flattening is refused.

Obstruction O5 (Q4 stays imported).
None of L4 or L5 promotes Q4 to a residual polynomial. The only residual polynomial with written coefficients remains

P(x) = x^5 - 4880 x^3 + 32879 x - 243

Gal(P/Q) = S5 and End(Jac(C_P)) = Z stay as previously locked. End_res(C_Q4) does not mix into residual P.


7. What is not claimed

- End(Jac(C_Q4)) is not locked.
- Igusa invariants are not computed.
- A period matrix is not computed.
- D5 Jacobian tables from the literature are not imported.
- Q4 is not residual-forced from 4880, 21, or 539 COUNT.
- Twin Prime is not claimed.
- RH is not claimed.
- No continuum, bounce, Fermi, or 539.9 s statement is made.
- Bridge C is not reopened.
- Necessity remains paused.
- Option 3 free 539-object claim remains paused.


8. Status code

END_RES_C_Q4_DEFINED_PACK_S_2026-08-31

The residual endomorphism object End_res(C_Q4) is defined from the written cover polynomial Q4 = x^5 + 20x - 32 and from Pack+(S) leftover combinatorics already on Master. Operations are the group-ring operations of Z[D5 x C2] acting on the residual permutation-sign module. First locks: L1-L7. First obstructions: O1-O5. Classical End(Jac) remains extra-input and is not this object.

Residual Discrete Algebra is what the leftover pieces are. Resonant Algebra is how long you choose to stir. They touch without merging. A count is not a clock. Residual-flux provenance mandatory.
