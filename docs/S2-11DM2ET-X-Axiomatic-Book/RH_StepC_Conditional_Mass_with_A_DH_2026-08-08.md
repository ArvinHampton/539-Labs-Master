# RH Step C — Conditional Mass-with-A under Named Density Hypothesis DH (2026-08-08)

**Status:** `RH_OPEN_DEBT_ARGUMENT_NOT_A_PROOF` · pure Category A · ZLA  
**Does not prove:** DH · Iso_H · unconditional Mass-with-A · B_θ · RH  
**Locked implications:** (RM)+(Iso_H)⇒B_θ · (RM)+(polylog StripDens)⇒Mass-with-A (ND1)

---

## 1. Named hypothesis DH(β⋆, C)

**DH(β⋆, C).** There exists C < ∞ such that for all T ≥ 2,

N(β⋆, T) := #{ ρ = β+iγ : β ≥ β⋆, 0 < γ ≤ T } = O((log T)^C).

This is a classical density-type hypothesis at fixed abscissa β⋆ > 1/2. It is **not** proved for any β⋆ ≤ 4/5 by current zero-density technology (Step B gap table).

---

## 2. Template implication (ZLA-clean)

Assume:
- (RM) — residual-mass / explicit-formula package as already fixed in the RH programme notes;
- DH(β⋆, C) for the abscissa β⋆ of a rightmost zero on a positive-density height set.

Then, on that set,

A ≪ M log T with M = O((log T)^C) ⇒ A = O((log T)^{C+1}) (polylog A)

⇒ Mass-with-A (programme definition)

⇒ B_θ under the already-recorded ND1 chain when the remaining (RM) hypotheses of that chain hold.

**Proof content:** only the already-proved programme implications plus the definition of DH. No new analysis of zeros of ζ is claimed.

---

## 3. What DH is not

| Object | Relation to DH |
|--------|----------------|
| Iso_H | Stronger isolation; DH does not imply Iso_H |
| Classical Ingham/CS density | Weaker (positive T-power); does not imply DH at moderate σ |
| On-line typical Omega | Orthogonal; does not give DH |
| Residual algebra / 539 | Forbidden as lemmas about ζ (ZLA) |

---

## 4. Programme use

1. Keep DH explicitly **conditional** in every theorem environment.
2. Use Step B table to show why DH is the right named gap at moderate σ.
3. Do not silently replace DH by classical density.

---

## 5. Status

- Step C template written.
- DH unproved at moderate σ.
- Mass-with-A unconditional: **open**.
- RH: **open**.

**Status code:** `RH_STEP_C_CONDITIONAL_MASS_WITH_A_DH_2026-08-08`

*Per aspera ad astra.*
